# Teleprompter

Standalone cross-device teleprompter. Read on phone/iPad, control from computer.

## Develop

    npm install
    npm run dev          # static site dev server
    npm run worker:dev   # signaling worker dev server (separate terminal)
    npm test             # unit tests

## Build

    npm run build        # outputs dist/
